{
    "application": {
        "serviceName": "Pagination",
        "shortServiceName": "Pagination",
        "port": "$APP_PORT",
        "logsPath": "$APP_LOGS_PATH",
        "stackVisibleError": "$STACK_VISIBLE_ERROR"
    }
}
